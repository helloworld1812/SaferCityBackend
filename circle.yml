machine:
  services:
    - docker

dependencies:
  pre:
    - pip install
    - pip install docker-compose

test:
  override:
    - docker-compose up -d
    - docker-compose run safercity-backend npm test
